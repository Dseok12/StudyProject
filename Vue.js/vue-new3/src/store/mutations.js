export default {
  SET_NEWS (state, news) {
    state.news = news
  },

  SET_ASK (state, ask) {
    state.ask = ask
  },

  SET_JOBS (state, jobs) {
    state.jobs = jobs
  },

  // Show, Newest 내가 연습하는것
  SET_SHOW (state, show) {
    state.shows = show
  },

  SET_NEWEST (state, newest) {
    state.newests = newest
  },
  // Show, Newest 내가 연습하는것

  SET_USER (state, user) {
    state.user = user
  },

  SET_ITEM (state, item) {
    state.item = item
  }
}
